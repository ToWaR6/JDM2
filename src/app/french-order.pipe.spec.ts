import { FrenchOrderPipe } from './french-order.pipe';

describe('FrenchOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
